# Hestia 场景监督的阶段对齐：仅标签 oracle 诊断

新增诊断名 `hestia-scene-phase-alignment-20260912-001` 经检索未使用。本地
初始场景一致性检查已完成，固定近邻未改善开合时间及前 50 槽左腕 MAE。
本项补充分离“绝对时间错位”与“阶段内几何映射”的影响，仍在用户本轮
本地检查范围内，不涉及模型、policy 训练或远端计算。

仅比较一个测量轴：绝对时间与基于**真实目标事件**的阶段坐标。读入刚保存的
45 场景前 100 帧动作；不再读 cache。几何距离、3-NN、donor/split 及 Action
Contract 全部继承原场景检查并按 SHA 冻结，不改 k 或搜索窗口。

- clock 控制：固定参考帧 50，使用 offsets −20…+9，即原 frame 30–59。
- event oracle：每个场景的参考帧为该场景左夹爪首次达到 0.5 的实测时刻，
  使用同一 offsets；有删失或任一槽越界则停止，不补齐、不缩窗。
- 两臂均由相同初始图像坐标选择相同 train donor；分别对两种目标坐标计算
  3-NN 均值、允许 donor 的均值和中位数，train 排除自身。每个方法在对应
  坐标内使用完全相同目标/槽数。关节与夹爪分开，不把不同物理单位合并。
- 主读数是左腕角与左关节的 MAE 相对同坐标常数基线的比值；同时完整报告
  左右关节、左右夹爪及全部绝对 MAE。两个坐标下目标分布不同，单看 oracle
  的绝对误差下降不能证明输入信息更可读；夹爪事件附近改善还部分来自对齐定义。
- Query 的真实开合时间参与 oracle 目标构造，开发/留一结果都不是可部署
  预测；更不是把此时间当作可用模型输入。不得用它选 checkpoint 或宣称策略改善。

原始数组只读，新目录 `runs/hestia-scene-phase-alignment-20260912-001/`。
源码/输入/容器摘要在执行前固定；WSL Bash → 既有离线 Linux Docker，2 CPU、
2 GiB，最多 60 秒。对齐越界/非 finite/身份漂移停止。仅验证所需的新对齐
反例和既有近邻/留一算术；无新模型、数据下载、hidden 或 Gate 3/4。
