# Hestia 八个 V 投影逐层互逆预登记

名称 `hestia-v-layer-20260912-001` 已检索无重名。目前仅本地准备，未上传或
执行，需下一个明确的带卡 SSH 窗口。上一轮 K/V 分组已完成：V-only 对完整
左关节退化的正向恢复/互逆损伤为 59.1%/69.7%，K-only 为 19.5%/13.8%。
V 在多个组有更大贡献，但 K 未被排除，hybrid 仍未修复泛化。

本轮单轴是替换的 V 投影层。先两个原生端点，再按固定层序
1、3、5、7、9、11、13、15，对每层运行两个方向：

- `base1280_v{layer}_640`：1280 只把该层 v_proj.weight 换为 640。
- `base640_v{layer}_1280`：640 只把同一 tensor 换为 1280。

两个原生端点各完整 180 次输出必须在本次新工作区精确复现历史数组，才允许
16 个干预条件。每次必须恰好改变一个登记键，shape 320×320、dtype 一致、
finite、donor 逐值一致；其余参数摘要不变，全部参数在推理期间不变。
八个键恰好覆盖上一轮 V-only 的八键，不含 K、其他层或其他动作参数。
不采用看到部分结果后挑下一层的自适应流程，八层及两个方向全部保留。

模型、checkpoint、数据、40/5 split、frame0、四个固定 noise、50 槽、
processor、normalization、Action Contract、原 RTX 4090 D 与登记运行时
保持一致。不加载 hidden、不训练、不写合成权重、不选择 checkpoint。

共 18 条件、3240 forwards、0 optimizer。上一轮每项约 120–127 秒，估计
全部工作约 40–43 分钟；每项仍限 180 秒，共享工作限 2700 秒（45 分钟），
受保护结束限 3000 秒（50 分钟）。沿用独立 watchdog、同一 shutdown helper、
Trash/其他 worker/GPU 检查，不释放实例。该新预算不从上一轮 25 分钟授权
窗口中挪用，需新窗口后执行。

结果预计比上一轮更大，仍不得超过既有 64 MiB 清单限额；登记回传宽限由
120 秒改为 240 秒，但不得超出 3000 秒外层上限。接收端先验证短连接和
stream 源文件可用，再启动守护器。使用短连接检查闭合 manifest 就绪，
就绪后才拉取完整流，不依赖长连接心跳维持十几分钟。任何失败接收包保留，
重试用全新目录；逐路径/类型/长度/SHA 全部核验后发送匹配收据。

分析仍报告 train/dev、完整/首动作、左右关节/夹爪、命令 FK 位置与朝向，
保留四个噪声与全部开发场景。各层分别计算 D、正向恢复、互逆损伤及交互，
与已完成的 V-only 联合效果对照；单层效果之和不等于联合效果时报告残差，
不假装参数效应线性可加。只有双向且四噪声一致才能支持该层对对应退化的
因果贡献，episode 13/wrist_angle 保留为预先指定的定位项。
不凭一次单层改善将某层命名为全部泛化失败的唯一根因。

新 helper/collector/supervisor/stream/verifier 和两份测试均使用新路径。
131 项本地检查通过，包含旧检查与 18 路真实 CLI 合成前置链、每层单键
替换、非法 donor 原子拒绝、缺失端点拒绝和 18 条件传输范围。
测试仅使用固定离线 Linux Docker、2 CPU/2 GiB，未构造真实策略。

从 `20260912T123514Z-bd007f5402bf-671150fc3285` 作为只读基线派生新工作区，
只上传九个新源码/测试/计划文件；旧实验、接收失败、数据和 normalization
证据保留，旧绑定不复制进新工作区。无新下载或原始样本/权重上传。
当前仍未确定唯一根因，M2 与 Gate 状态不变。
