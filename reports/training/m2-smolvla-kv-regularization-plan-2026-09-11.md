# 已保存视觉 K/V 特征的正则化诊断

2026-09-11，登记 `contextual-kv-regularization-20260911-001`。
接续用户的本地视觉泛化排查；只在已有 Linux Docker 中对已保存数组作 CPU
读出分析，不加载策略权重、原始数据或 hidden，不运行 SSH、训练或仿真。

## 假设与唯一变量

上次早层/末层读出的 alpha=100 位于搜索上限。40 个独立训练场景、2560 维
特征使近乎精确的训练拟合不足以证明读出泛化能力。检验“扩大 ridge 正则化
搜索范围能否改善该冻结表示的泛化”，不把它当作策略训练的 weight decay 实验。

固定早层 1、2×2 池化、标准动作首步、原 40 train / 5 development 顺序，
固定 seed 20260911、原 train-only 特征标准化、ridge 公式与混合单位 MAE
选择代理。早层是历史控制臂，不依据本轮结果选择层。

- 控制搜索 `[0.001, 0.1, 1, 10, 100]`。
- 处理搜索在控制基础上加 `[1000, 10000, 100000, 1000000, 100000000]`。
- 保持原混合单位选择代理以隔离搜索范围；关节与夹爪始终分组报告。
- 全 train 五折选择最终 alpha；dev5 只评价最终两个读出，不参与选择。
- 两臂另外作相同五外折/五内折 nested CV；每个外折仅用其训练部分选择
  alpha、拟合标准化与均值/中位数基线。内层 seed=20260911+外折编号+1。
  外折留出预测不做图像错配解释。该 CV 仍是同一开发数据上的诊断，不是
  新数据，也不是对历史训练流程完全独立的泛化验证。

## 判读、完整性与预算

扩大搜索有用须看关节/夹爪各自的 nested CV、dev5 误差及训练拟合变化；
dev5 必须同时优于控制和 train 构造的两个常数基线、且正确图优于全部非自身
错配图的平均 MAE，才支持这一个切片的读出改善。epsilon=1e-8。
只趋近常数不算获得有用视觉映射；若处理仍选上界，记录搜索边界，不自动扩展。
两个分组全部报告，不根据结果改变指标、特征、标签、随机种子或网格。

先核验历史 result/features/source SHA；重现历史早层全预测（绝对容差 1e-10）；
输出新目录，禁止覆盖。保留 folds、选值、曲线、按场景/动作组误差和 effective
ridge degrees of freedom。输出数组 exact reload。不把数组 reload 当模型 reload。

先通过 check_env、Ruff 和泄漏/算术 synthetic tests，然后运行一次分析。
固定已有镜像 digest（同名 JSON），CPU 2 核，内存及 memory+swap 均 2 GiB，
网络关闭，NumPy BLAS 线程数 2，分析上限 180 秒，输出上限预估 2 MB。
identity drift、非 finite、预算超限或历史预测不一致立即停止并保留日志。
不下载、不安装依赖，不打开后续实验。实现/输入 SHA 在运行前写入同名 JSON。

这只能检验当前线性读出对正则化的敏感性，不能证明信息缺失、标签必然有噪声，
或增加示范/训练步数必然有效。Zen 特征不代表 coverage40 B 的动作专家；
policy improvement、Gate 3/4 和 task success 均未测，M2 未完成。
