# Coverage40 执行修订 002

用户已明确以修复视觉忽略为目标，授权自主安排后续工作，不再等待逐项指示。
保留 001 失败现场；新的工作区、job 及阶段 run name 使用 002 身份。
本次仅修复计划落盘与真实入口的兼容性，训练轴仍为固定总更新下场景覆盖 8→40，
不改 optimizer 数值、冻结范围、模型/processor/Action Contract、评估或验收阈值。

执行计划改用原生 YAML 保存，生成后立即通过真正的 `_resolve_plan()` 重新读取和
校验，并要求完整对象逐值相等。四阶段全部通过后才完成 prepare；不再只对内存
字典执行 schema 检查。新增回归覆盖旧 JSON 科学计数法失败、四阶段 YAML 完整
入口及非法数字字符串拒绝。历史 YAML reader、原生 trainer 和 hash-bound
历史控制源码均保持原样，不将历史计划重新格式化。

控制 A 复用已校验迁移路径；已存在时要求该路径仍精确指向同一 checkpoint，
保留源件。B 的模型、plan、run name 与 checkpoint 路径统一从 002 计划派生。
新输出为 `runs/visual-coverage40-unattended-002/`。

共享计算上限仍为 30 分钟，内存及预留磁盘不变。各阶段失败依旧立即终止计算，
不由脚本重试；为支持本轮有人值守的错误分析，失败后仅保留空闲实例至同一个
截止时间，然后执行既有保护检查和关机。不会在失败后延长截止或继续训练。
如另一明确登记的任务正在运行，关机保护会拒绝干扰该任务并记录原因。
模型失败与工程失败分别记录；下一实验需按新证据单独登记，hidden 不解封。

本修订文档不是结果。目标仍未完成；后续必须以实际预检、参数更新、完整 reload
和固定视觉对照证据判断，不以启动成功或 loss 下降宣告修复。
