# Hestia 图像 V 更新分量：共同偏移是已测后期退化的主要贡献

十条件、1800 次 policy forward、3600 次额外小型 donor 线性投影已完成，0 optimizer，耗时 1195.953 秒。全部 52 文件、14,213,120 字节归档回传并通过 SHA256 校验，匹配收据已送回。独立复算 95,744 个字段通过。两个原生端点与两个完整图像替换对照共 720 次输出精确复现既有数组；未修改模型参数。

本轮将八个奇数层真实图像位置的 V640−V1280 输出差，拆成训练集共同均值、逐 token 共同模式和场景残差。共同均值只使用 train40 的图像 V 差，不使用动作答案或开发集统计。完整前缀仍为 241 个位置，donor 使用完整原生 CUDA GEMM；只修改真实图像的前 64 个位置，其他位置保持原输出。部分分量使用 F32 加法后转 BF16；完整图像对照直接复制 donor 输出。分量效应不可简单相加。

下表为完整开发轨迹的正向恢复 / 互逆损伤，占原 640→1280 正退化量的比例。先按噪声配对计算误差，再取均值。这些比例不是任务成功率，也不是全部泛化差距的解释比例。

| 指标 | 完整图像 V 差 | 共同均值 | token 模式 | 场景残差 |
|---|---:|---:|---:|---:|
| 左关节 | 47.74% / 50.78% | 35.87% / 33.84% | 5.00% / 10.90% | 6.20% / 5.02% |
| 右关节 | 15.00% / 18.21% | 26.52% / 23.64% | −7.05% / 0.11% | −6.15% / −3.97% |
| 左夹爪 | 41.46% / 24.08% | 35.90% / 19.52% | 7.70% / 5.25% | −1.03% / −1.39% |
| 右夹爪 | 42.21% / 43.99% | 42.74% / 42.48% | 10.34% / 9.84% | −11.59% / −7.82% |

共同均值在以上四组以及左右命令朝向上均为四个噪声条件一致的双向有害更新。右关节和双夹爪的场景残差却在两个方向都表现为有益更新：回滚会损伤，注入会改善。因此，不能把所有场景相关更新一概解释为过拟合，也不能把本轮结果解释为视觉几何信息全部被抹除。共享 V 偏移仍可通过注意力权重及后续查询反馈影响不同场景，不能直接等同于固定动作偏置。

左关节原生 1280 MAE 为 0.061672967 rad，回滚共同均值后为 0.060537865 rad；原生 640 为 0.058508082 rad，train mean 常数为 0.047658 rad。四个噪声条件下全部十条件仍未同时击败左关节的两个常数基线。共同均值恢复的是后期增量中的约 35.9%，完整泛化问题在 640 时已经存在。现有结果确定了一个后期退化机制，但没有确定全部视觉泛化失败的唯一根因。

身份与证据：

- run：`hestia-image-value-component-20260912-001`；workspace：`20260912T151222Z-bd007f5402bf-46803787f6a4`。
- template SHA256：`090d81a2e46e57377896086582a811a02a3414998b70d918fefc393f80aa3dc8`。
- 回传 manifest SHA256：`cae7302ea1282c6e999420e80f6f0fdf1c1b6c22f92c1185126d808c029774cc`。
- 远端证据：`runs/hestia-image-value-component-recovered-20260912-001/verified/`。
- 主分析、独立复算、夹爪事件：`runs/hestia-image-value-component-analysis-20260912-001/`，入口分别为 `verified/result.json`、`independent-verification.json`、`events.json`。
- 152 项执行前本地检查、85 项远端身份检查及 4 项后处理测试已通过；后处理使用固定离线 Linux Docker、2 CPU、2 GiB、0 本地模型 forward。

平台界面在本轮完成后已实测目标实例关机；本轮 shutdown-request 尚未回传，应在下次已授权窗口只读恢复，今晚不为此重开实例。前一轮 value-route 的请求和匹配收据已另行恢复，见同日期 `value-route-shutdown-recovery` 报告。关机观察不等同于账单审计。

遵照用户“一小时内收尾、明天继续”的最新安排，今晚结束计算，下一步转向 320/640 阶段已经存在的映射误差。保留全部失败和旧身份，不重跑完成实验，不选择新 checkpoint、不开展新训练。FK 只表示命令位姿，夹爪事件保留右删失语义；无新 Gate 3/4，M2 未完成。
