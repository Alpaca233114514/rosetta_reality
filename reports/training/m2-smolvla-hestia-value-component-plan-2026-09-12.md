# Hestia 原生 V 输出更新分量双向干预预登记

运行名 `hestia-value-component-20260912-001` 已检索无重名。用户在完成逐层校验后明确要求根据结果继续找根因，并说明时间充裕，不再询问 SSH。范围为本实例、既有 CUDA 环境和缓存、零 optimizer 的有界诊断；不得启动训练、读取 hidden、下载、释放实例或发布。旧逐层结果和失败证据保持不变。

逐层互逆显示退化分布于多个 V 层，第 7 层完整左关节恢复仅 17.52%。开发集场景协方差及整体偏移都参与，训练与开发响应相反。现有第 1/15 层的 BF16 感知 CPU 近似发现更新能量主要是跨场景共同成分，但近似不是原生 CUDA 精确重放，能量不是动作因果贡献。因此本轮直接干预实际 CUDA V 输出，而非按能量大小判因果。

唯一实验轴：八个奇数层 V 输出更新的分量。固定十条件：原生 1280、原生 640；1280 接入完整旧 V 输出、640 接入完整新 V 输出；两个方向各自仅接入全局均值、token 共同模式、场景残差。层集合、640/1280 checkpoint、frame0、train40/dev5、四噪声、50 槽、数据/processor/Action Contract 和原 CUDA 运行时全部固定。

每层在原生 CUDA BF16 autocast 内，用现有输入和 donor 矩阵计算真实 donor 投影。每行每层只计算一次 donor；对其余 10 去噪 × 4 噪声的调用断言输入及未干预 V 输出完全不变。保存每个非 hidden 行的输入/互逆差值 SHA；跨所有条件必须与首次原生采集吻合。零的符号位在哈希中归一，以免互逆相减的 +0/−0 被误判为数值变化。

令 Δ=V640(x)−V1280(x)。全局分量 g 是训练 40 行、全部 64 token 的均值；token 分量 t 是训练行的逐 token 均值减 g；场景残差 r=Δ−g−t。只将训练行交给均值计算，不使用动作标签或开发均值。均值使用 float64 累加再存 float32。反向干预使用相反符号。部分干预以 float32 加入原输出，再转原 BF16；完整干预直接返回 donor 输出，避免舍入来回导致伪差异。零参数修改，全部 500 参数前后摘要不变。V 干预可能改变后续 query/attention，不宣称固定注意力。

两个原生端点必须精确复现历史数组；两个完整 V 输出对照必须精确复现已回传的 K/V split V-only 参数置换数组。前四个对照任一失败，停止后续分量干预，不放宽容差。校准文件使用 create-only 写入并绑定原生 result.json 中的 SHA，各后续条件验证 seal；45 行全部观测，但只有前 40 行用于分量均值。

十条件 × 180 = 1800 policy forwards，额外 3600 次小型 donor V 线性投影，0 optimizer。原始模型及权重不导出修改版本。每条件 180 秒，工作总限 1800 秒，受保护结束总限 2100 秒，回传宽限最多 240 秒且不超过外层。峰值模型预算仍为 CUDA 4 GiB / RSS 5 GiB；追加缓存约 60 MiB，不靠扩容或 offload。沿用独立 watchdog 与原受保护关机 helper，结果落 durable storage，接收器短连接等待，64 MiB 回传上限，不释放实例。

按训练/开发、完整/首动作、左右关节/夹爪、命令 FK 位置/朝向报告全部分量与四噪声、全部开发场景；保留相关常数基线。比较各分量双向恢复/损伤、联合效应与单独效应不可加残差，并重复 scene MSE 和夹爪事件分析。只有双向四噪声一致才支持对应指标的因果贡献；即使某分量解释大部分后期退化，也不能自动解释 640 已存在的泛化失败，更不能宣布 M2 成功或唯一根因。

在新 content-addressed 工作区执行，以已完成逐层工作区为只读基线。只增量传新源码、测试、计划；完整 V-only 参考数组从同一实例的已校验旧工作区 create-only 复制并核对固定 SHA，无新样本/权重上传。全部本地合成检查通过后才启动该阶段。
