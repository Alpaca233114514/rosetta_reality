# Local image-encoding reuse registration — 2026-09-09

The user authorized a more efficient local investigation chain. The JSON
companion freezes this extension, the completed uncached Zen report and its
original diagnostic registration before execution. Both historical baseline
results remain unchanged. This adds one 160-forward comparison, without
training, checkpoint selection, downloads, SSH or simulation.

Reuse exact prepared-image embeddings only inside one eval-mode VLM and a
no-gradient context. Keys bind pixel bytes, shape, device, dtype and autocast;
parameter identity/version changes fail closed. Retain the actual embeddings
for masked placeholder cameras. Do not replace them with zeros or change
attention masks. A 64 MiB LRU bounds retained embeddings; leaving the context
restores the original embedding method. This opt-in diagnostic wrapper is not
installed in the trainer or Gate runners.

Run the unchanged temporal diagnostic on the same Zen artifact, 20 train
observations and four noise settings. The fixed Linux/XPU Docker image stays
offline, with 6 GB memory/swap and two CPUs, batch one, and a 20-minute budget.
The ordinary ingress, sample-identity and finite-chunk checks still run.

Accept acceleration correctness only when every recorded first action matches
the uncached reference exactly in all 14 dimensions, with all provenance and
sample identities equal. Full predicted chunks are checked for finiteness by
the original diagnostic; this comparison does not record full-chunk parity.
Report cache hits, actual encoder calls, peak cache bytes, complete elapsed
time and their ratio even if acceleration is ineffective. A single sequential
comparison includes model loading and host variation and is not a controlled
performance benchmark. Output files are create-only. Any error preserves its
log and does not authorize a training run or an automatic OOM retry.

Preflight: 68 synthetic tests passed. The separate retained-code reproduction
first failed because its script lacked the source path, then passed with only
that entrypoint corrected. Both logs are retained. Ruff check and format check
passed for the nine affected Python files.
