# Hestia K/V 分组互逆预登记

名称 `hestia-kv-split-20260912-001` 已检索无重名。仅完成本地准备，未上传或
执行。上一轮联合 K/V 互换对七个完整轨迹退化指标显示四噪声一致的双向
因果贡献，但 hybrid 仍未修复泛化；权威见同日期 parameter-crossover-result。

本轮单轴为“替换的参数子集”：八个 K-only 与八个 V-only。固定顺序：

1. `base1280`：180 次原生输出精确复现已回传 CUDA 1280。
2. `base640`：180 次原生输出精确复现已回传 CUDA 640。
3. `base1280_k640`：1280 仅替换奇数层八个 k_proj weight 为 640。
4. `base640_k1280`：640 仅替换同八个 k_proj weight 为 1280。
5. `base1280_v640`：1280 仅替换奇数层八个 v_proj weight 为 640。
6. `base640_v1280`：640 仅替换同八个 v_proj weight 为 1280。

两组键严格不相交，合并恰好为原登记十六键；层为 1/3/5/7/9/11/13/15，
shape 320×320，dtype/finite/每项 donor 值精确核验。变动键集合必须恰好
为对应八键，其余全部参数逐字节不变，推理前后全部参数摘要不变。
checkpoint、dataset、processor、normalization、runtime、Action Contract、
45 个 frame0、四个历史 noise、50 槽全部沿用。hidden 不读取、不训练、不落盘
合成权重。两原生端点必须在本次新工作区完整精确通过，才准进入四项干预。

六组共 1080 forwards、0 optimizer，每项仍 180 秒；按上一轮每项约
122–129 秒估计总工作约 14 分钟。登记工作上限 1200 秒、受保护结束上限
1500 秒。上限变化来自六组而非四组，需带卡 SSH 窗口确认后执行；没有
沿用旧四组的窗口去启动新轴。GPU 仍只允许原 RTX 4090 D，资源与停止门禁
不变，独立 watchdog、Trash/其他 worker 检查及受保护关机 helper 不变。

接收器先连接并启用 SSH keepalive，失败接收目录不得覆盖；完整结果必须
先回传并校验，再发送匹配收据。120 秒失败回传宽限仍保留，外层 watchdog
不依赖本地连接。002 的关机请求记录未取回，应在下一次连接先只读恢复；
旧 shutdown/空接收器状态保留，不以 SSH 断连证明计费或释放状态。

分析固定沿用联合互换的 train/dev、左右关节/夹爪、首动作/完整轨迹、命令
FK 位置/朝向，以及全部四噪声/开发场景。对 K-only 和 V-only 分别计算
D、正向恢复、互逆损伤及交互项。仅双向且四噪声一致的组才能支持该参数
子集对相应后期退化的因果贡献；联合效果不等于两个单独效果之和，若依赖
同时替换则继续查 K/V 相互作用。episode 13 的 wrist_angle 是额外定位项，
不得丢掉其他场景或将局部改善推广为唯一根因。

实现使用新的 helper/collector/supervisor/stream/verifier，不修改原 001/002。
预检覆盖真实 CLI 六分派、端点缺失拒绝、输入篡改/无卡拒绝、两个八键子集
划分、非法 donor 原子拒绝、参数范围和结果清单；69 项本地检查通过。
全部在固定离线 Linux Docker、2 CPU/2 GiB，未构造真实 policy。

从已登记 `20260912T113534Z-bd007f5402bf-1cd3c7fc0c28` 作为只读基线派生
全新内容寻址工作区，只传九个新代码/测试/计划文件。保留旧数据和失败证据，
不重新传原始数据/权重/14 文件包，不复制旧 normalization 绑定。
未获下一次窗口前不上传、不启动推理。没有新 Gate 3/4，M2 与根因目标未完成。
