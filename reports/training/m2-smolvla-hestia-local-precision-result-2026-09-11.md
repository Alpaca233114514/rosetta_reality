# Hestia本地重复稳定，但BF16/FP32均不等价于原CUDA对照

两次独立模型加载，每次16次forward（episode49、四噪声、BF16/FP32各重复
两次），共32次，全部完成。每次500参数张量前后逐SHA不变；同进程重复
normalized/standard数组逐值相同，两进程生成的四数组NPZ文件SHA也完全相同：
`6dde0aea0e30616369cf7e5c1ba2c0f1a9a55bf93246551bd4fcc96763fc3936`。
每次第一份BF16零噪声还逐值复现了上次保存的XPU失败数组。

对原CUDA BF16记录的active14维差异如下；四条件范围不是置信区间：

| 本地模式 | normalized最大绝对差范围 | normalized平均绝对差范围 | standard平均绝对差范围 |
|---|---:|---:|---:|
| BF16 | 0.027344–0.037109 | 0.005119–0.006119 | 0.001141–0.001383 |
| FP32 | 0.028997–0.041893 | 0.006729–0.007808 | 0.001456–0.001746 |

两个模式在四种噪声下均未通过原先冻结的跨设备容差；FP32的平均差异全部更大，
没有通过换精度解决。未调整容差，也未把稳定重复误称为CUDA等价。

已排除这一个训练样本上“每次加载/调用随机变化”及推理修改参数的解释；
结果与运行后端/精度路径差异一致，但未精确定位到某个算子，不能单凭它证明
所有设备差异均来自某个BF16内核。只加载一张训练图；数值上下文预检仍涉及
45个非hidden frame0，不称只访问一行数据。开发图像和hidden未读取。

每进程约89秒，XPU峰值allocated约1.30GB；Ruff/格式通过。原跨设备失败与
quarter停止边界保留。后续若要解释原CUDA训练过程，应在原CUDA环境先逐值
复现1280控制，再采集320/640/960；本地目前不能代替这项同设备证据。

没有新的optimizer、Gate、远端连接或实例启动。证据位于
`runs/hestia-local-precision-20260911-001/`及同名前缀preflight目录，同名JSON绑定SHA。
