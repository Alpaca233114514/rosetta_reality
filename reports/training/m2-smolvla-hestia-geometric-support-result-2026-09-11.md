# Hestia：二维位置越界不足以解释开发失败

固定45张缓存图像与Hestia C原始输入逐张匹配，两种图像摘要均通过；蓝色
socket质心、bbox、面积也与既有提取逐值一致。所有操作在本地离线CPU容器
完成，远端保持关机（用户已确认），没有加载模型、执行optimizer或读取hidden。

开发集22/13/7/33/45均位于训练质心的轴范围内，除13外其余4个在训练凸包内。
二维近邻距离超过训练留一95%分位数的为13和33；五维图像代理的5个场景都
在参考距离内。二维/五维的参考值分别为0.471583/2.246132。
这些只刻画可见socket代理，不能证明完整3D姿态、两物体关系或动力学覆盖。

固定1-NN按图像几何选训练donor，完整复制其目标chunk；不使用dev标签选邻居。
下表为完整chunk开发MAE，关节为rad、夹爪为合同标准单位。

| 固定读出 | 关节 | 夹爪 | 左臂关节 | 左夹爪 |
|---|---:|---:|---:|---:|
| 质心2D 1-NN | 0.068780 | 0.069759 | 0.065446 | 0.079112 |
| 图像代理5D 1-NN | 0.077852 | 0.104976 | 0.075541 | 0.127220 |
| 两种train常数中的较小误差 | 0.050389 | 0.053093 | 0.047658 | 0.051472 |
| C四噪声平均 | 0.061944 | 0.066347 | 见JSON | 见JSON |

两种近邻的train留一完整chunk误差也均未胜过常数。5D末动作关节有局部收益
（0.079818，对照0.081339），但同时夹爪为0.474715，对照0.210170；不能据此
判定整体修复。2D与5D的首动作也未同时胜过常数。

因此，不能用socket二维位置越界充分解释开发集失败；直接复制几何最近样本
也不是有效修复。近邻失败并不证明图像无法预测动作：稀疏采样、遗漏其他物体
位置、非线性映射或时序分歧均尚未被区分。下一步应检查这些代理与动作之间
是否存在可插值的对应关系，再决定是否值得更改模型空间表示。

执行证据：`runs/hestia-geometric-support-20260911-001/`；预检证据：
`runs/hestia-geometric-support-preflight-20260911-001/`。Ruff、格式检查通过；
3项凸包边界、训练缩放隔离及留一近邻反例通过，输出数组reload逐值一致。
同名JSON保留完整分组指标及原始证据SHA。未执行Gate 3/4，M2仍未完成。
