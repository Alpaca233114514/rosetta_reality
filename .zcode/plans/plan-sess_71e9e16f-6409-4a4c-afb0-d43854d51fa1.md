# Zen 炉收尾全修计划(文档补记 + 首偏差追踪预注册 + AutoDL 收尾)

范围 = 你确认的三项遗留:①架构文档补记 ②下一个单轴预注册(取 Zen 审计 §10 选项 3:用 **firstaction artifact** 做首偏差追踪——唯一现在就可执行、且审计按安全画像推荐的轴;T2 recovery 数据需先有通过 teacher gate 的教师,时序权重复现被审计 §9 明示低价值)③AutoDL 收尾(artifact 回传 + 关机)。

## Step 1 — 远端状态核实 + selected deploy artifact 回传

- 单条 SSH 只读检查 `rosetta-autodl`(开机状态、GPU 占用、tmux、磁盘、`/root/zen-runtime` 与 durable 盘下两臂 deploy artifact 位置)。
- **若实例已关机**:AutoDL 开机只能由你在控制台操作,我会停下报告,等你开机后继续(回传是 AGENTS.md 强制项,值得一次短开机)。
- 定位 `m2-smolvla450m-zen-cuda-b64-{uniform,firstaction}-001-step0316-deploy-001`(约 1.2GB × 2),单流 scp + retry 回传(防代理掐杀连发连接)到本地 `runs/m2-smolvla450m-aloha-insertion-action-repair-bounded-gripper-003/artifact_backup/`(该目录已建但为空),逐文件 SHA256 对照远端 manifest,写本地回传清单 json(create-only)。

## Step 2 — 关机(执行你的常设授权指令)

- 确认 nvidia-smi 无占用、无训练进程/tmux 会话 → 远端裸 `shutdown`(rc=255 属正常),验证重连被拒。
- 实例**不释放**:8 个 checkpoint 在系统盘 `/root/zen-runtime/checkpoints`,关机保留数据,仅释放才销毁。
- trace 首选本地容器执行,故回传后立即关机最省计费;若 Step 4 本地跑不动再开机走远端(会先报告)。

## Step 3 — Zen 首偏差追踪:预注册 → 冻结 → 执行

- 新建 `reports/training/m2-smolvla-zen-first-deviation-preregistration-2026-08-28.{md,json}`:复用 Aster 已登记协议(train episode 2 / env seed 10 / noise seed 10 / ≤320 步,专家回放与策略同起点对照),假设=量化 Zen firstaction 的 step-0 与早期状态偏差并与 Aster 基线(step-0 动作 MAE 0.0204、状态 MAE step1>0.01/step4>0.025)对比;非 gating 诊断;冻结 artifact manifest sha、episode/seed、代码身份、停止条件。
- 新建 create-only `scripts/diagnose_smolvla_zen_trajectory.py`(适配自 `diagnose_smolvla_aster_trajectory.py`:episode-split 校验指向 Zen firstaction 实验配置、输出命名 `zen-trajectory-*`;处理器文件名用 glob 解析,避开审计缺陷 1 的 step_5/step_7 硬编码)和 `configs/vla/smolvla_450m_aloha_insertion_zen_trace_sim_001.yaml`(以 `aster_trace_sim_007.yaml` 为模板,指向 firstaction deploy artifact)。不动任何冻结历史脚本。
- **Commit 1 + push**(在 `codex/smolvla-training-harness-v2` 分支)冻结预注册,然后才执行。
- 在 WSL Docker 容器(`scripts/run_m2_container.sh vla-xpu`)对本地回传的 artifact 执行 trace,产出 `runs/<exp>/diagnostics/zen-trajectory-*.json`。预计 CPU 推理约 30–90 分钟;若容器内不可行(OOM 等),fallback = 你开机后经 stage r43 在远端跑,先报告再动。

## Step 4 — 结果报告 + 文档补记 + Commit 2

- 新建 `reports/training/m2-smolvla-zen-first-deviation-trace-2026-08-28.{md,json}` 如实记录结果(无论好坏)。
- 更新 `docs/m2-smolvla-architecture.md`:头部日期与证据快照;§1 阅读链插入 Zen 审计为最新证据源;§2 状态表加 Zen 双臂行(Gate 4 第 4/5 次失败、最新 selected checkpoint=uniform step316 MAE 0.02157);§4 ownership map 登记 Zen 七件套脚本;§5 配置链加 Zen r42b 分支;§8 状态机加 Zen 行、**如实记录 411 有两项失败准则**(minimum_task_success_rate + joint_limits_respected;审计 MD §6"唯一失败准则"的文字疏漏在此更正记录,不改不可变审计)与本次 trace 结果;§9 T1 行更新(时序权重在 batch64/316 规模被否定、Aster 增益不可跨 regime 外推);§10 下一序列改为 Zen 审计 §10 三选项;记录 FFmpeg 负结果、durable 盘 50G 事件与 checkpoint 系统盘位置。
- 更新 workspace `AGENTS.md` M2 边界段落(Faust 单次失败 → 五身份连败现状,证据源链指向 Zen 审计)和 `README.md:79-82` 状态句;`docs/architecture.md` 仅链接不改。
- **Commit 2 + push**。

## Step 5 — memory 更新(仓库外,不提交)

- `zen-v2-harness-trial.md`:七件套已随 d75d1c9 入库、正式炉 r42b 完成定论、trace 结果、关机状态。
- `autodl-access-and-transfer.md`:如回传过程有新教训则补充。

## 明示不做

FFmpeg/torchcodec 升级(审计登记押后到专门维护会话)、T2 recovery 数据炉(需先过 teacher gate,另行设计)、任何新训练/改参重跑、远端 durable 盘清理(下炉前任务)、编辑不可变历史审计或 runs 证据。

## 风险与决策点

- 实例若已关机:我无法代你开机,Step 1 起点改为"报告并等你开机"。
- 1.2GB×2 过代理 SSH 回传可能较慢(retry 稳态单流,预计十几分钟级)。
- 本计划的批准即视为对 trace 执行、两次 commit+push、以及回传后关机的授权;任何偏离(如需开机跑远端 trace)都会先报告。