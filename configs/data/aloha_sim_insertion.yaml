name: aloha_sim_insertion_human_episode_0
source: lerobot_v3
repo_id: lerobot/aloha_sim_insertion_human
revision: main
episodes: [0]
cameras:
  top: observation.images.top
fields:
  state: observation.state
  action: action
  timestamp: timestamp
  instruction: task
  episode_index: episode_index
  frame_index: frame_index
embodiment: aloha
chunk_size: 8
cache_root: data/lerobot
license: MIT
expected:
  frames: 500
  state_dim: 14
  action_dim: 14
  instruction: Insert the peg into the socket.
