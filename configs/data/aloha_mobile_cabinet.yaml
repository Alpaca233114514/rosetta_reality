name: aloha_mobile_cabinet_episode_0
source: lerobot_v3
repo_id: lerobot/aloha_mobile_cabinet
revision: main
episodes: [0]
cameras:
  high: observation.images.cam_high
fields:
  state: observation.state
  action: action
  timestamp: timestamp
  instruction: task
  episode_index: episode_index
  frame_index: frame_index
embodiment: mobile_aloha
chunk_size: 8
cache_root: data/lerobot
license: Apache-2.0
expected:
  frames: 1500
  state_dim: 14
  action_dim: 14
  instruction: Open the top cabinet, store the pot inside it then close the cabinet.
